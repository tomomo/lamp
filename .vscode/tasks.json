{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build",
      "type": "shell",
      "command": "docker-compose -f containers/docker-compose.yml build && docker images | grep -E '^lamp-(php-fpm|apache|mysql|mariadb) '",
      "problemMatcher": []
    },
  ]
}
