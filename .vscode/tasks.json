{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "EXAMPLE: docker-compose up -d",
      "type": "shell",
      "command": "docker-compose -f example/docker-compose.yml up -d",
      "problemMatcher": []
    },
    {
      "label": "EXAMPLE: docker-compose down",
      "type": "shell",
      "command": "docker-compose -f example/docker-compose.yml down",
      "problemMatcher": []
    }
  ]
}